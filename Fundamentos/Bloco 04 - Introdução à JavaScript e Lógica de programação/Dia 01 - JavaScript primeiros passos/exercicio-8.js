const primeiroNumero = 13;
const segundoNumero = 7;
const terceiroNumero = 11;

let numeros = false

if (primeiroNumero % 2 === 0 || segundoNumero % 2 === 0 || terceiroNumero % 2 === 0 ) {
    numeros = true;
    
};

console.log(numeros);